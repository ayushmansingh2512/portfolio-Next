// schemaTypes/index.ts
import { projectType } from './project'

export const schemaTypes = [projectType]

// If you have other schemas, add them here:
// export const schemaTypes = [projectType, otherSchemaType, anotherSchemaType]